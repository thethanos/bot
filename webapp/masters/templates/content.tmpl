{{define "content" -}}
<h2>{{.Header}}</h2>
{{range $index, $item :=.Masters}}
    <div class="column">
        <div class="card">
            <div class="pics">
            {{range $index, $image := .Images}} 
                <img src="{{$image}}" alt="" style="width:100%">
            {{end}}
            </div>
            <div class="container">
                <h2>{{$item.Name}}</h2>
                <p>{{$item.Description}}</p>
                <p><button class="button">Cвязаться</button></p>
            </div>
        </div>
    </div>
{{end}}
{{end}}